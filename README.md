# Empdetails
A MVC application using servlets (controller)and JSP(view)
The purpose of the controller is to hold the business logic,once it gets the data from the model it sends the data to the view page and presents the data
We are using JSTL to access the data
